<span class="swiss-flag" aria-hidden="true">
  <span class="swiss-flag__fabric">
    <svg viewBox="0 0 32 32" width="32" height="32" focusable="false">
      <path fill="#da291c" d="M0 0h32v32H0z" />
      <path fill="#fff" d="M13 6h6v7h7v6h-7v7h-6v-7H6v-6h7z" />
    </svg>
  </span>
</span>

<style>
  .swiss-flag { display: inline-block; flex: none; width: 2rem; height: 2rem; perspective: 120px; }
  .swiss-flag__fabric { position: relative; display: block; width: 100%; height: 100%; overflow: hidden; border-radius: 1px; transform-origin: left center; animation: flag-wave 3.6s ease-in-out infinite; }
  .swiss-flag__fabric::after { position: absolute; inset: 0; background: linear-gradient(105deg, transparent 10%, rgb(255 255 255 / 24%) 35%, rgb(0 0 0 / 12%) 60%, transparent 85%); background-size: 220% 100%; content: ''; animation: flag-light 3.6s ease-in-out infinite; }
  @keyframes flag-wave {
    0%, 100% { transform: rotateY(-12deg) skewY(2deg); }
    50% { transform: rotateY(12deg) skewY(-2deg); }
  }
  @keyframes flag-light {
    0%, 100% { background-position: 100% 0; }
    50% { background-position: 0 0; }
  }
  @media (prefers-reduced-motion: reduce) {
    .swiss-flag__fabric, .swiss-flag__fabric::after { animation: none; }
  }
</style>
